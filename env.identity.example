# Public EduGame Portal origin only. No secrets.
# `.env*` files are gitignored; copy this value into `.env.local` for local runs.
# Leaving it unset disables Account v0 (identity chrome shows "not configured",
# progress stays in memory only).
NEXT_PUBLIC_EDUGAME_IDENTITY_ORIGIN=http://localhost:3000
